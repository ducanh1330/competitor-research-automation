# <Workflow Name>

## Objective
What this produces and why. One or two sentences.

## Inputs
| Input | Required | Notes |
|---|---|---|
| e.g. `url` | yes | Ask the user if not provided |

## Steps
1. **<Step>** — run `tools/<name>.py --arg <value>`
   - Writes: `.tmp/<file>`
2. **<Step>** — ...

## Output
Where the deliverable lands (Sheet/Slides/Doc URL), and what "done" looks like.

## Edge cases & failure handling
- **<Symptom>** → <what to do>

## Learned constraints
Append here as you discover them — rate limits, quotas, auth expiry, pagination
gotchas. Date each entry.
